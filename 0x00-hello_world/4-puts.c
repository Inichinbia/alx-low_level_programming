#include<stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	 /*
	  * This is void because no integer involved
	  * main - This is Entry point
	  * puts is used to print output
	 * The return 0 indicates successful execution
	 */
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
