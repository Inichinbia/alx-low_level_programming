#include<stdio.h>
/**
 * main - Entry point
 * returns 0
 */
int main(void)
{
	/* This program demonstrates the usage of the main function
	 * and the puts
	 * The return value 0 indicates successful execution
	 */
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
